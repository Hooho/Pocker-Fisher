# Agent 工作规则

## Git 提交

- 每完成一个独立功能或用户明确要求的代码变更，且相关检查通过后，自动创建一次 Git 提交，不等待用户再次提醒。
- 提交前先检查工作区状态，只暂存本次任务实际修改的文件；不得把用户已有的无关改动、临时产物或密钥提交进去。
- 提交标题和正文使用中文，标题保持简洁明确，可使用 Conventional Commits 前缀，例如 `feat: 完善赛事流程`、`fix: 修复存档校验`、`chore: 整理项目目录`。
- 提交正文应简要说明变更内容、变更原因和验证结果；如果变更较小，也至少在提交标题中说明具体做了什么。
- 提交后确认提交成功，并检查工作区状态；如果仍有未提交内容，说明其来源。
- 没有实际文件变更时，不创建空提交。

## 分支与发布流程

- `develop` 是日常开发分支，后续功能开发、问题修复和测试验证都必须在 `develop` 上进行。
- `main` 只用于合并已经验证通过的发布内容，不直接在 `main` 上开发。
- 准备发布时，先在 `develop` 完成测试和构建，再将 `develop` 合并到 `main`，由 `main` 执行发布流程。
- 合并到 `main` 前必须确认工作区干净、测试通过，并检查版本号、构建产物和发布渠道配置。
- 如果 `develop` 尚不存在，应从当前稳定的 `main` 创建；创建后默认切换到 `develop` 开发。

## 文件与安全

- 禁止批量删除文件或目录。
- 删除文件时，一次只能删除一个路径明确的文件；如果确实需要批量删除，先列出范围并请求用户授权。
- 不允许私自登录远程服务器；如需登录，先向用户请求权限。
- 在远程服务器上的任何操作，都必须先向用户请求权限。
